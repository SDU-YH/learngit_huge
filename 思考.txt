   自动缩放&资源分配
1.	决策器（状态空间？回报函数？状态转移概率？）
2.	信息收集器（当前系统的资源利用率，吞吐量，负载情况）
3.	执行器（怎么执行当前的决策，执行什么样的决策（横向扩展？纵向扩展？））

1.	负载模拟器（负载的类型）
2.	部署的web应用（怎么在云中部署web应用？）

1.	衡量标准（tail latency，response time，开机延迟（整合机器））
2.	Benchmark（baseline 静态分配 峰值分配 按预测分配）


性能满足slo时，最小化成本（x）